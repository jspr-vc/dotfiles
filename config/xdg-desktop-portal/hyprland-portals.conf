# Which portal backend answers which interface under Hyprland.
# Hyprland's portal does screencast and global shortcuts; everything else,
# file chooser included, goes to the GTK portal.
[preferred]
default=hyprland;gtk
org.freedesktop.impl.portal.FileChooser=gtk
